apiVersion: v1
kind: Secret
metadata:
  namespace: avatar
  name: api
type: Opaque

data:
  # python -c "import secrets; print(secrets.token_hex(), end='')" | base64
  pepper: TODO
  # python -c "import secrets; print(secrets.token_hex(), end='')" | base64
  authjwt_secret_key: TODO
  # python -c "import base64; import os; print(str(base64.urlsafe_b64encode(os.urandom(32)), encoding='utf-8'), end='')" | base64
  file_encryption_key: TODO
